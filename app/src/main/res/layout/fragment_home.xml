<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <!-- AI Content Generator Button -->
    <Button
        android:id="@+id/btnAiContentGenerator"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="AI Content Generator"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginBottom="16dp" />

    <!-- AI Image Background Remover Button -->
    <Button
        android:id="@+id/btnStoryGenerator"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Generate visual story with AI"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/btnAiContentGenerator" />

</androidx.constraintlayout.widget.ConstraintLayout>